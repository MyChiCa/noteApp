<view class="cell">
    <view class="cell-item" wx:for="{{array}}" wx:key="item.id">
        <text class="cell-title">{{item.name}}</text>
        <image src="../../../image/icon-right.png"></image>
    </view>
</view>
